# VIC Emergency Monitor Configuration
# Copy this file to .env and fill in your values

# Optional: Azure Maps API key for more accurate geocoding
# Get one at: https://portal.azure.com -> Create Azure Maps resource
AZURE_MAPS_API_KEY=

# Optional: Google Maps API key (alternative geocoder)
GOOGLE_MAPS_API_KEY=

# Polling interval in seconds (default: 3600 = 1 hour)
POLL_INTERVAL=3600

# Output format: table, json, csv
OUTPUT_FORMAT=table

# Optional: Webhook URL to send alerts on status changes
WEBHOOK_URL=

# Optional: Email notifications (requires SMTP config)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
ALERT_EMAIL=
